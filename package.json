{
  "name": "googleid-verification",
  "version": "1.0.0",
  "description": "Vercel dashboard to match product variants against Google Shopping listings via SerpApi for manual verification.",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "vercel dev",
    "deploy": "vercel deploy --prod"
  },
  "dependencies": {
    "@upstash/redis": "^1.38.0"
  }
}
