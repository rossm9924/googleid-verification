{
  "name": "googleid-verification",
  "version": "1.0.0",
  "description": "Cloudflare Worker dashboard to match product variants against Google Shopping listings via SerpApi for manual verification.",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "start": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.97.0"
  }
}
